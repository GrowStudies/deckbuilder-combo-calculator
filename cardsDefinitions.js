/* Determines the values of a card in the combo.
For example: 
* Deneb = normal summon + high/low scale
* ROTA = Search Deneb (search path to combo, treated differently since can ash)
* PSY-Frame Driver = nothing, is a monster
* Gate Zero = Low Scale
Notes: if you require specific discard fodder you must specify
*/

